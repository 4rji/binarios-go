{
  "name": "openstack-lab-portal",
  "version": "0.1.0",
  "private": true,
  "description": "OpenStack lab launcher web and Electron app.",
  "main": "electron/main.cjs",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --workspace backend run dev",
    "dev:web": "npm --workspace frontend run dev -- --host 0.0.0.0",
    "dev:desktop": "electron .",
    "start:api": "npm --workspace backend start",
    "build:web": "npm --workspace frontend run build",
    "build:desktop": "npm run build:web && electron-builder",
    "test": "npm --workspace backend test"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "electron": "^42.1.0",
    "electron-builder": "^26.8.1"
  },
  "build": {
    "appId": "dev.openstack.labportal",
    "productName": "OpenStack Lab Portal",
    "files": [
      "electron/**/*",
      "frontend/dist/**/*",
      "package.json"
    ],
    "directories": {
      "buildResources": "electron/assets",
      "output": "dist-desktop"
    }
  }
}
